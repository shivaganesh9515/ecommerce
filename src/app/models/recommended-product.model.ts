export interface RecommendedProduct {
      title: string;
  store: string;
  image: string;
  price: string;
  originalPrice: string;
  rating: number;
  badge: string | null;
  badgeClass: string;
  reviewCount: number;
}
